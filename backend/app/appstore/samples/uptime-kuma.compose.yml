services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3001}:3001"
    volumes:
      - /opt/app-data/uptime-kuma:/app/data
